/**
 * Live AI version stamp. Bump this whenever the live BotController's decision
 * logic changes in a way worth benchmarking against the previous frozen
 * snapshot (see tools/sim-runner/baselines/vN/). The matching frozen snapshot
 * self-declares its own version and must NOT import this module.
 */
export const AI_VERSION = 1;
